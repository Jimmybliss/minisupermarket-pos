(function (window) {
  // Runtime environment for the Angular app.
  // Deployments can overwrite this file to change runtime settings without rebuilding.
  window.__env = window.__env || {};

  // Default backend base URL. Update this in your deployment (or overwrite this file)
  // to point at the API root. Example: 'https://mini-supermarket-2.onrender.com'
  window.__env.BASE_URL = window.__env.BASE_URL || 'https://mini-supermarket-2.onrender.com/';

  // Add other runtime values here if needed, for example:
  // window.__env.SOME_FLAG = window.__env.SOME_FLAG || 'value';
}(this));
